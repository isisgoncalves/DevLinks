function toggleMode() {
  const html= document.documentElement
  html.classList.toggle("light")

  //*Alterando imagem com JS=
  //*Pegar a tag img:
  //  const img = document.querySelector("#profile img")
  //*Substituir a imagem:
  //  if(html.classList.contains("light")){
  //*Se tiver light mode, adicionar essa imagem:  
  //    img.setAttribute("src", "./local da imagem")
  //*Se não estiver no light mode, manter a imagem normal:
  //  } else {
  //    img.setAttribute("src", "./local da imagem")
  //  }
}